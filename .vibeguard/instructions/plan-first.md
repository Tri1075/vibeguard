<!-- OWNER-EDITABLE — agents must never modify this file. -->
# Plan before you build

A project starts with a robust action plan, not code. Before the first implementation step, propose a plan and save it as PLAN.md: goal, ordered milestones, risks, and how each milestone is validated. Keep it current — when reality diverges, update the plan and say so; never drift from it silently.
