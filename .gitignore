node_modules/
dist/
*.tgz
coverage/
.driftguard/snapshots/
.driftguard/state/
.driftguard/backups/

# NOTE: plugin-bin/ (the bundled plugin binaries from scripts/bundle-plugin.mjs)
# is intentionally COMMITTED — a Claude Code plugin is cloned, not built, so the
# self-contained binaries must ship in the repo. Rebuild with `npm run
# bundle:plugin` and re-commit at each release.
